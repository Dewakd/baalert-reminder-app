{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "pet",
  "description": "Capability for the Windows and Linux desktop pet overlay",
  "windows": ["pet"],
  "permissions": [
    "core:default",
    "core:window:allow-set-position",
    "core:window:allow-set-size",
    "core:window:allow-start-dragging"
  ]
}
